{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx prisma migrate deploy && npx prisma generate && nodemon index.js",
    "start": "npx prisma migrate deploy && npx prisma generate && node index.js",
    "db:deploy": "npx prisma migrate deploy && npx prisma generate"
  },
  "keywords": [],
  "author": "Dharani I S",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "body-parser": "^2.2.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "nodemon": "^3.1.9",
    "prisma": "^6.6.0"
  }
}
