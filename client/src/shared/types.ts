export interface PlanType {
    name: string,
    description: string,
    price: string,
    features: Array<string>,
}

export interface TestimonialType {
    quote: string,
    longquote: string,
    name: string,
    profession: string,
}